# Backend API base URL (no trailing slash). Used by the API client.
# Development: scenario-backend runs on http://localhost:4000
# Copy to .env.local and adjust if your backend runs elsewhere.
NEXT_PUBLIC_API_URL=http://localhost:4000
